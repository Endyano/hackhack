# Google Calendar OAuth connect + sync. Deterministic (no AI involved) --
# oauth.py handles the auth flow, client.py talks to the Calendar API,
# store.py persists tokens, sync.py orchestrates fetch -> upsert -> prune.
